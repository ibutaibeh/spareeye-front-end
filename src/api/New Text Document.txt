ملف apiClient.js هو بمثابة "المحرك" الذي يدير كل طلبات الـ HTTP في مشروعي.

بدلاً من أن نكتب في كل مكوّن:


axios.post('http://localhost:5000/api/requests', data, { headers: { Authorization: ... } })
وتعيد كتابة نفس الإعدادات (الرابط، الترويسة، قراءة التوكن، معالجة الأخطاء…) في كل مكان، apiClient يوفّر نسخة واحدة جاهزة من axios فيها كل شيء مضبوط تلقائيًا:

الفوائد الفعلية:
تجميع الإعدادات في مكان واحد

baseURL (رابط الـ API الأساسي) محدد مرة واحدة فقط.

إذا تغيّر الرابط (مثلاً في بيئة الإنتاج)، تغيّره في هذا الملف فقط.

إضافة التوكن تلقائيًا

يقرأ التوكن من localStorage قبل أي طلب ويضيفه إلى ترويسة Authorization.

لا داعي لتمريره يدويًا في كل طلب.

معالجة الأخطاء مركزية

أي خطأ من السيرفر يتم تحويله لرسالة موحّدة (message) يمكنك عرضها مباشرة في الواجهة.

يقلّل من تكرار كود فحص الأخطاء في كل مكوّن.

تحسين الأمان والنظافة

يمنع أخطاء التكرار (مثل نسيان إضافة ترويسة أو وضع رابط خاطئ).

الكود في المكوّنات يصبح أنظف وأبسط.

💡 النتيجة: أي مكوّن يحتاج طلب API سيكتب فقط:


import apiClient from '../api/apiClient';

const response = await apiClient.post('/auth/sign-in', { email, password });
وسيحصل على:

baseURL مضبوط.

Authorization مضاف إذا كان التوكن موجود.

معالجة أخطاء جاهزة.